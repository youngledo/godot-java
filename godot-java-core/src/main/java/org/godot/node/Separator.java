package org.godot.node;

import java.lang.foreign.MemorySegment;

public class Separator extends Control {
	Separator(MemorySegment nativePointer) {
		super(nativePointer);
	}

	Separator(long nativePointer) {
		super(nativePointer);
	}
}

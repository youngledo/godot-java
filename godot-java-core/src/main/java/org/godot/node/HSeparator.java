package org.godot.node;

import java.lang.foreign.MemorySegment;

public class HSeparator extends Separator {
	HSeparator(MemorySegment nativePointer) {
		super(nativePointer);
	}

	HSeparator(long nativePointer) {
		super(nativePointer);
	}
}

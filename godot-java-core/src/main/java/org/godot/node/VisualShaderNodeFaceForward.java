package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeFaceForward extends VisualShaderNodeVectorBase {
	VisualShaderNodeFaceForward(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeFaceForward(long nativePointer) {
		super(nativePointer);
	}
}

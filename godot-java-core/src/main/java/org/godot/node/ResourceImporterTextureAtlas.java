package org.godot.node;

import java.lang.foreign.MemorySegment;

public class ResourceImporterTextureAtlas extends ResourceImporter {
	ResourceImporterTextureAtlas(MemorySegment nativePointer) {
		super(nativePointer);
	}

	ResourceImporterTextureAtlas(long nativePointer) {
		super(nativePointer);
	}
}

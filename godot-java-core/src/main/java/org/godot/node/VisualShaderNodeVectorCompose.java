package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeVectorCompose extends VisualShaderNodeVectorBase {
	VisualShaderNodeVectorCompose(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeVectorCompose(long nativePointer) {
		super(nativePointer);
	}
}

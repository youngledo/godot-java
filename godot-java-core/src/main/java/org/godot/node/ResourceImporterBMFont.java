package org.godot.node;

import java.lang.foreign.MemorySegment;

public class ResourceImporterBMFont extends ResourceImporter {
	ResourceImporterBMFont(MemorySegment nativePointer) {
		super(nativePointer);
	}

	ResourceImporterBMFont(long nativePointer) {
		super(nativePointer);
	}
}

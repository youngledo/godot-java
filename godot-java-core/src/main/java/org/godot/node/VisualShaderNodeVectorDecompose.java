package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeVectorDecompose extends VisualShaderNodeVectorBase {
	VisualShaderNodeVectorDecompose(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeVectorDecompose(long nativePointer) {
		super(nativePointer);
	}
}

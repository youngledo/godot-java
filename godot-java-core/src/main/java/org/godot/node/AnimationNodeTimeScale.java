package org.godot.node;

import java.lang.foreign.MemorySegment;

public class AnimationNodeTimeScale extends AnimationNode {
	AnimationNodeTimeScale(MemorySegment nativePointer) {
		super(nativePointer);
	}

	AnimationNodeTimeScale(long nativePointer) {
		super(nativePointer);
	}
}

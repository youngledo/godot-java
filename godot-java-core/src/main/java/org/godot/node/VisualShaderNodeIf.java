package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeIf extends VisualShaderNode {
	VisualShaderNodeIf(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeIf(long nativePointer) {
		super(nativePointer);
	}
}

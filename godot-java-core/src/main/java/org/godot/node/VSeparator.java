package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VSeparator extends Separator {
	VSeparator(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VSeparator(long nativePointer) {
		super(nativePointer);
	}
}

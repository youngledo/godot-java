package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeWorldPositionFromDepth extends VisualShaderNode {
	VisualShaderNodeWorldPositionFromDepth(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeWorldPositionFromDepth(long nativePointer) {
		super(nativePointer);
	}
}

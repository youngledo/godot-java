package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeTextureSDF extends VisualShaderNode {
	VisualShaderNodeTextureSDF(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeTextureSDF(long nativePointer) {
		super(nativePointer);
	}
}

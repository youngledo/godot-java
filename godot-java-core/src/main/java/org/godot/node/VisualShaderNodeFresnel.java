package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeFresnel extends VisualShaderNode {
	VisualShaderNodeFresnel(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeFresnel(long nativePointer) {
		super(nativePointer);
	}
}

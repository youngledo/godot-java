package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeDotProduct extends VisualShaderNode {
	VisualShaderNodeDotProduct(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeDotProduct(long nativePointer) {
		super(nativePointer);
	}
}

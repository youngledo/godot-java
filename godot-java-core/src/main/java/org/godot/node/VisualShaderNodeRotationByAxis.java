package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeRotationByAxis extends VisualShaderNode {
	VisualShaderNodeRotationByAxis(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeRotationByAxis(long nativePointer) {
		super(nativePointer);
	}
}

package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeTextureParameterTriplanar extends VisualShaderNodeTextureParameter {
	VisualShaderNodeTextureParameterTriplanar(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeTextureParameterTriplanar(long nativePointer) {
		super(nativePointer);
	}
}

package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeVectorLen extends VisualShaderNodeVectorBase {
	VisualShaderNodeVectorLen(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeVectorLen(long nativePointer) {
		super(nativePointer);
	}
}

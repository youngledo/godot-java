package org.godot.node;

import java.lang.foreign.MemorySegment;

public class ShaderGlobalsOverride extends Node {
	ShaderGlobalsOverride(MemorySegment nativePointer) {
		super(nativePointer);
	}

	ShaderGlobalsOverride(long nativePointer) {
		super(nativePointer);
	}
}

package org.godot.node;

import java.lang.foreign.MemorySegment;

public class GLTFDocumentExtension extends Resource {
	GLTFDocumentExtension(MemorySegment nativePointer) {
		super(nativePointer);
	}

	GLTFDocumentExtension(long nativePointer) {
		super(nativePointer);
	}
}

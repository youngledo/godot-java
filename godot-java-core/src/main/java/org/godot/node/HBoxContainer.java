package org.godot.node;

import java.lang.foreign.MemorySegment;

public class HBoxContainer extends BoxContainer {
	HBoxContainer(MemorySegment nativePointer) {
		super(nativePointer);
	}

	HBoxContainer(long nativePointer) {
		super(nativePointer);
	}
}

package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeLinearSceneDepth extends VisualShaderNode {
	VisualShaderNodeLinearSceneDepth(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeLinearSceneDepth(long nativePointer) {
		super(nativePointer);
	}
}

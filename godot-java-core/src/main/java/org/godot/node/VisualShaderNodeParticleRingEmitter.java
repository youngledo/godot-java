package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeParticleRingEmitter extends VisualShaderNodeParticleEmitter {
	VisualShaderNodeParticleRingEmitter(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeParticleRingEmitter(long nativePointer) {
		super(nativePointer);
	}
}

package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeScreenUVToSDF extends VisualShaderNode {
	VisualShaderNodeScreenUVToSDF(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeScreenUVToSDF(long nativePointer) {
		super(nativePointer);
	}
}

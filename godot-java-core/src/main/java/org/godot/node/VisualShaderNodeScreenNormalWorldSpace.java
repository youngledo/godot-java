package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeScreenNormalWorldSpace extends VisualShaderNode {
	VisualShaderNodeScreenNormalWorldSpace(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeScreenNormalWorldSpace(long nativePointer) {
		super(nativePointer);
	}
}

package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeVectorRefract extends VisualShaderNodeVectorBase {
	VisualShaderNodeVectorRefract(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeVectorRefract(long nativePointer) {
		super(nativePointer);
	}
}

package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeRandomRange extends VisualShaderNode {
	VisualShaderNodeRandomRange(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeRandomRange(long nativePointer) {
		super(nativePointer);
	}
}

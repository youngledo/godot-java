package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeVectorDistance extends VisualShaderNodeVectorBase {
	VisualShaderNodeVectorDistance(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeVectorDistance(long nativePointer) {
		super(nativePointer);
	}
}

package org.godot.node;

import java.lang.foreign.MemorySegment;

public class ResourceImporterImage extends ResourceImporter {
	ResourceImporterImage(MemorySegment nativePointer) {
		super(nativePointer);
	}

	ResourceImporterImage(long nativePointer) {
		super(nativePointer);
	}
}

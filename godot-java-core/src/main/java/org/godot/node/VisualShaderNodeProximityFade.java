package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeProximityFade extends VisualShaderNode {
	VisualShaderNodeProximityFade(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeProximityFade(long nativePointer) {
		super(nativePointer);
	}
}

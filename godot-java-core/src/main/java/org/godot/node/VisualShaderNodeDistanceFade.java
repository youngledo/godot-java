package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeDistanceFade extends VisualShaderNode {
	VisualShaderNodeDistanceFade(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeDistanceFade(long nativePointer) {
		super(nativePointer);
	}
}

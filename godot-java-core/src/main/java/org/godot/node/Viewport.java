package org.godot.node;

/**
 * A Viewport that can be used as a tree root. Hand-written stub — full
 * implementation to be added.
 */
public class Viewport extends Node {

	protected Viewport(long nativeObject) {
		super(nativeObject);
	}

	protected Viewport(java.lang.foreign.MemorySegment nativePtr) {
		super(nativePtr);
	}

	protected Viewport() {
		super();
	}
}

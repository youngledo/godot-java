package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeVaryingGetter extends VisualShaderNodeVarying {
	VisualShaderNodeVaryingGetter(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeVaryingGetter(long nativePointer) {
		super(nativePointer);
	}
}

package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeVaryingSetter extends VisualShaderNodeVarying {
	VisualShaderNodeVaryingSetter(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeVaryingSetter(long nativePointer) {
		super(nativePointer);
	}
}

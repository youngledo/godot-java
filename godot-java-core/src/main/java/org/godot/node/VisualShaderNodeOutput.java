package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeOutput extends VisualShaderNode {
	VisualShaderNodeOutput(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeOutput(long nativePointer) {
		super(nativePointer);
	}
}

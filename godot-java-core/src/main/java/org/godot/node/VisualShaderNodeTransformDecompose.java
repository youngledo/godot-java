package org.godot.node;

import java.lang.foreign.MemorySegment;

public class VisualShaderNodeTransformDecompose extends VisualShaderNode {
	VisualShaderNodeTransformDecompose(MemorySegment nativePointer) {
		super(nativePointer);
	}

	VisualShaderNodeTransformDecompose(long nativePointer) {
		super(nativePointer);
	}
}
